version: "2"
services:
  automation:
    image: archmisha/mediarss_automation
    ports:
      - "8080:8080"
    expose:
      - "8080"
    volumes:
      - /docker_data/tomcat-data:/usr/local/tomcat/logs